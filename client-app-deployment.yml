kind: Deployment
apiVersion: apps/v1
metadata:
  name: client-app
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: client-app
  template:
    metadata:
      labels:
        app: client-app
    spec:
      containers:
      - name: client-app
        image: "hosnikadour/frontend-app-iteslab:latest"
        imagePullPolicy: Always
        ports: 
         - containerPort: 3000
        
---
apiVersion: v1
kind: Service
metadata:
  name: client-app-service
  namespace: default
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      nodePort: 31000
  selector:
    app: client-app